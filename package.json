{
  "name": "slash-command-action",
  "version": "2.0.0",
  "private": true,
  "description": "Check issue or pull request comments for `/commands` and trigger or block workflows based on them.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts --source-map --license license.txt",
    "jest": "jest --coverage",
    "lint": "tsc --noEmit",
    "package": "ncc build src/main.ts --source-map --license license.txt",
    "release": "npm run package && git add -f dist/",
    "test": "npm run lint && npm run jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/step-security/slash-command-action.git"
  },
  "keywords": [
    "actions"
  ],
  "author": "step-security",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^2.0.0",
    "@actions/github": "^8.0.0",
    "axios": "^1.20.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.1",
    "@vercel/ncc": "^0.38.3",
    "jest": "^30.5.1",
    "jest-circus": "^30.2.0",
    "nock": "^14.0.17",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
